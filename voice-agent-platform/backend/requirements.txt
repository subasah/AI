fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0
python-dotenv>=1.0.1
loguru>=0.7.2
pyyaml>=6.0.2
python-multipart>=0.0.12
pymysql>=1.1.1
cryptography>=43.0.0

# Optional — install when you are ready for live voice / telephony
# Default speech-to-speech path (Gemini Live Flash):
#   pip install "pipecat-ai[google,daily,silero]"
# Classic cascade (Deepgram + OpenAI + Cartesia):
#   pip install "pipecat-ai[daily,deepgram,cartesia,openai,silero]"
# twilio>=9.0.0
# mcp>=1.0.0
