# Incoming call handler

Python package lives at `../incoming_call_handler/` (underscores required for imports).

Entry: `incoming_call_handler/handler.py` — Twilio `/voice/incoming` + WebRTC bootstrap.
