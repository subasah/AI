# Car dealer industry pack

Template swarm: greeter → sales / service.

Wire DMS tools via MCP (`dms`) or HTTP:

- `search_inventory`
- `schedule_test_drive`
- `get_service_appointment_slots` (secret ref: `CUSTOMER_DMS_API_KEY`)
