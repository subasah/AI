# Production build for GitHub Pages
# Leave empty to call same-origin /api (won't work on github.io alone).
# Point at your hosted FastAPI when ready, e.g. https://api.yourdomain.com
VITE_API_BASE=
VITE_ADMIN_TOKEN=dev-admin-token
